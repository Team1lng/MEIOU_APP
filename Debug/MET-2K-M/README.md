<!--
 * @Author: error: error: git config user.name & please set dead value or install git && error: git config user.email & please set dead value or install git & please set dead value or install git
 * @Date: 2024-07-12 08:27:58
 * @LastEditors: error: error: git config user.name & please set dead value or install git && error: git config user.email & please set dead value or install git & please set dead value or install git
 * @LastEditTime: 2024-12-16 10:39:17
 * @FilePath: /Doorbell/README.md
 * @Description: 这是默认设置,请设置`customMade`, 打开koroFileHeader查看配置 进行设置: https://github.com/OBKoro1/koro1FileHeader/wiki/%E9%85%8D%E7%BD%AE
-->
# 更新说明

## 20240720
1.修改ADC采集键值

## 20240722
1.修改sync同步为fsync
2.修改默认开锁时间5s
3.修复夜间红外进入视频未切换夜视问题

## 20240801
1.修复呼叫灯光不匹配户型问题
2.调试通话效果

## 20240817
1.优化环形队列实现，添加接受阻塞标志位，使用原子操作函数
2.添加兼容魔力指纹模块实现

## 20240819
1.修复删除0号卡失败问题
2.优化make.sh中"add_audio_param_file"函数
3.添加室内机管理操作时禁用呼叫
4.添加铃声播放重载宏定义及调用(添加铃声播放重载宏定义及调用及添加音量默认值宏定义和环形队列初始化)
5.添加定时器时间参数判定规范，0秒不作用

## 20240820
1.优化指纹模块功能，完善自动注册模板

## 20240822
1.优化指纹触摸事件，上事件处理完成方可继续添加事件
2.修复视频发送报错，返回无效参数错误码，原因可能是代码中的某个部分错误修改了该地址结构或者是由于未定义的行为导致的内存损坏
    修复方式：每次发送前重新初始化一次struct sockaddr_in结构体

## 20240904
1.优化指纹模块，添加事件结构体事件释放原子变量，优化按下事件注册及调用
2.修改魔力模块颜色控制枚举定义，与黑火模块颜色控制枚举保持一致
3.添加ADC采集滤波

## 20240905
1.添加线程名设置
2.添加调音宏开关设置
3.添加SC92F836驱动模块中断使能配置

## 20241023
1.完善gpio配置接口，添加direction配置high、low选项
2.优化应用对引脚配置,如开锁、ircut、led、amp

## 20241104
1.优化呼叫忙线，当进入监控时，不再触发呼叫忙线超时定时器
2.优化呼叫按键灯光，呼叫按键灯光随按键变化，每次只会亮一个或全灭，当进入监控时，灯光保持常亮，当进入通话，则会亮对应户型灯光，直至通话结束

## 20241111
1.修复指纹门口机上电期间手指按压导致进入逻辑错误，后续指纹操作无响应问题;其根本原因是事件队列未初始化，导致新的事件无法插入，事件标志位未释放，导致下次指纹检测无响应
2.修复夜间进入监控红外灯常灭问题
3.优化视频采集接口，修复夜间上电视频采集进入夜间模式失败；原因：上电时视频采集未完成初始化仅切换夜视，导致夜视切换失败; 解决方案：将音视频采集初始化放到Epoll初始化前

## 20241114
1.优化CircularList队列初始化，添加读函数断言报错队列名打印，新添队列声明定义接口，并修改src相关队列定义代码
2.由于指纹模块固件修改，因此修改指纹模块中断检测逻辑，中断间隔半秒以上作为一次有效中断，关闭中断消抖延时
3.优化语音播放内存泄漏问题，VoiceRingPlayL接口中Info申请的内存且未释放，删除Info内存申请，Info指针已指向有效数组InfoList，再申请毫无意义
4.在VoiceInfoImport函数中Valid的标志位清除时，清除的变量不是InfoList数组里的。因此修改为指向InfoList数组指针的TmpInfo中Vaild变量
5.优化VoiceInfo结构体，将成员变量Vaild更改成原子类型，并修改相关引用
6.ReadFingerModuleData函数中Index超出FINGER_NUM_MAX会出现数组越界导致段错误，添加Index小于FINGER_NUM_MAX判断条件，不成立退出函数
7.指纹命令发送前清空串口数据FingerWriteCmd函数中添加UartClear接口调用，防止旧数据存在导致接收数据后解析到错误旧数据出现不可预知问题，如问题8
8.优化指纹命令接收接口FingerReadData，接收数据长度大于指纹数据数组最大时返回false;测试段错误根本原因！！！,分析错误包长，根据错误的包长，读取比缓存数组更多的数据，且串口缓存的数据多余缓存数组，因此导致数组越界
9.修复CircularListRead函数接口中，队列为空时退出未释放锁

## 20241203
1.优化视频采集接口，修复夜间上电视频采集进入夜间模式失败；原因：上电时视频采集未完成初始化仅切换夜视，导致夜视切换失败;解决方案:在视频采集时，判断当前夜间情况是否进入夜视，避免未能正常切换夜间模式

## 20241212
1.修复夜视重启后，白天状态下进入应用IRCUT未切换回白天状态；解决方案：上电时重新调用InfraredDetectHandle(DarkModeStatus())检测一次光敏判断白昼

## 20250102
1.优化定时器初始化，为了避免定时器使用受初始化流程影响，将定时器结构体数组静态赋值，在程序编译时完成初始化,并删除mian函数中Timer_init函数调用及删除该函数

## 20250315
1.定时器添加原子操作，防止二次删除竞争操作
2.优化循环队列源码，优化语音播放源码
3.添加获取本地设备ID接口
4.修复接收到视频通话消息未作校验确认是否对象是本机，将该校验逻辑补上修复该问题
5.优化视频采集夜视切换及I帧请求接口，将该功能更改为原子标志位，具体切换请求放到视频采集线程处理
6.在CircularListRequest申请节点时，避免节点在申请后未使用，导致下次申请失败，后续所有节点的申请都必须重新写入到队列中；此问题分别修改了语音播放、音频输出等相关队列
7.优化红外检测功能，红外中断触发后开启或刷新红外电平稳定检测定时器，定时器开启期间中断触发重新刷新计时，定时结束作一次有效检测，定时时间一秒

## 20250728
1.修改键值AD检测值
2.增加心跳检测，防止启动被load_isp_conf 损坏，
3.软件实现网卡检测保护（检测到网卡掉线后重装网卡）
4.拨码开关改为高低电平检测


## 20250903
1.修复接听时按键灯亮问题

## 20251107
1.指纹中断检测由上下沿触发改为高低电平检测
2.增加检测内核崩溃日志重启
3.未进监控关闭音频采集
4.未进监控内关闭视频采集

## 20251227
1.检测到指纹按压给指纹模块供电，平时待机断电